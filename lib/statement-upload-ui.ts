/**
 * Temporary public-UI gate for the statement-upload journey.
 *
 * Set this to `true` to restore the existing upload controls on `/analyse`
 * and both existing upload-focused Home sections (“How it works” and
 * “Security & privacy”).
 * This flag intentionally does not remove or alter any upload/API logic.
 */
export const STATEMENT_UPLOAD_UI_ENABLED = false
